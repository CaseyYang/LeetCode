#include<iostream>
#include<vector>
using namespace std;

struct TreeNode {
	int value;
	TreeNode* left;
	TreeNode* right;
};

vector<TreeNode*> func(TreeNode* root,int n) {
	TreeNode* cur = root;
	TreeNode* smaller = NULL, *bigger = NULL;
	while(cur!=NULL&&)
}

int main() {
	return 0;
}